// Production stand-in for devMock.js. The Vite alias swaps this in for
// production builds so the sample pipeline data never enters the bundle.
export const FIXTURE_MARKER = null;
export const HAS_FIXTURE = false;
export const MOCK_DATA = {};
export const MOCK_COLUMNS = {};
export const MOCK_CONFIG = null;
